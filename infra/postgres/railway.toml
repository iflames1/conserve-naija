# Railway Postgres service. Set the service root to `infra/postgres`.
# Attach a volume at /var/lib/postgresql/data.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
