# Copy to .env for local development.
ENVIRONMENT=local
DATABASE_URL=postgresql+asyncpg://conserve:conserve@localhost:5433/conserve_naija
CORS_ORIGINS=["http://localhost:3000"]

# Comma-separated (or JSON array). Any account signing up or signing in with one
# of these emails is granted the platform administrator role and organisation
# administration. No password is seeded here.
ADMIN_EMAILS=isaacomenuche@gmail.com
